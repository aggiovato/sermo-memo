package com.sermomemo.core.designsystem.theme

// Durations in milliseconds. docs/sermo-memo-master.md §39.
// Motion explains a change; it is never decorative or permanent, and it must honour the system's
// reduced-motion setting.
object SermoMotion {
    const val FAST = 100
    const val SHORT = 180
    const val MEDIUM = 280
    const val LONG = 420
}
